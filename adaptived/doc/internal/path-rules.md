## Rules for Paths in adaptived

`/foo` - only operate on the `/foo` directory

`/foo/` - only operate on the `/foo` directory

`/foo*` - operate on `/foo` and its children

`/foo/*` - only operate on `/foo`'s children
